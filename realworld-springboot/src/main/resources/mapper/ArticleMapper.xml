<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.realworld.api.mapper.ArticleMapper">
    <select id="findArticlesByAuthor" resultType="com.realworld.api.model.entity.Article">
        SELECT *
        FROM articles a
                 JOIN users u
                      ON a.created_by = u.id
        WHERE u.username = #{userName}
    </select>

    <select id="findArticlesByTag" resultType="com.realworld.api.model.entity.Article">
        SELECT *
        FROM articles a
                 JOIN articles_tags at ON a.id = at.article_id
                 JOIN tags t ON at.tag_id = t.id
        WHERE t.tag_name = #{tagName}
    </select>

    <select id="findArticlesByUserName" resultType="com.realworld.api.model.entity.Article">
        SELECT *
        FROM articles a
                 JOIN users_favorite_articles ufa ON a.id = ufa.article_id
                 JOIN users u ON ufa.user_id = u.id
        WHERE u.username = #{userName}
    </select>
</mapper>